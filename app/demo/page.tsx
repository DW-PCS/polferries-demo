import { AdminConfigDemo } from '@/components/AdminConfigDemo';

export const metadata = {
  title: 'Admin Config Demo - PolFerries',
  description: 'Demo showcasing admin configuration provider',
};

export default function DemoPage() {
  return <AdminConfigDemo />;
}
